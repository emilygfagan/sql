-- Import and Export
/* Practicing importing a csv file
directly into a table. First I have
to create a table that matches the
format of the csv file. */

CREATE TABLE simple(
a INTEGER,
b INTEGER,
c INTEGER
);

/* Uploaded simple csv file, running query
to view the complete table. */

SELECT * FROM simple;

-- Query successful